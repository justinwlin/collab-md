# CollabMd Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build a real-time collaborative markdown editor — Elixir/Phoenix relay server on Fly.io + CLI client + REST API. Ephemeral rooms with auto-versioned snapshots, all in-memory.

**Architecture:** Phoenix server holds room state in GenServers (one per room) behind a DynamicSupervisor + Registry. WebSocket via Phoenix Channels handles real-time sync. REST API for programmatic access (Claude Code, curl, scripts). CLI watches local `.md` file and syncs changes bidirectionally over WebSocket. No database — everything is ephemeral.

**Tech Stack:** Elixir 1.19, Phoenix 1.8 (API-only, no Ecto/HTML/LiveView), WebSockex (CLI WebSocket client), FileSystem (CLI file watcher), Fly.io

---

## File Map

```
collab_md/                          # Phoenix server (root)
├── lib/
│   ├── collab_md/
│   │   ├── application.ex          # Modify: add Registry + DynamicSupervisor
│   │   ├── room.ex                 # Create: GenServer per room
│   │   └── room_supervisor.ex      # Create: DynamicSupervisor + room creation
│   ├── collab_md_web/
│   │   ├── channels/
│   │   │   ├── room_channel.ex     # Create: real-time sync channel
│   │   │   └── user_socket.ex      # Modify: add channel route
│   │   ├── controllers/
│   │   │   ├── room_controller.ex  # Create: REST API
│   │   │   └── room_json.ex        # Create: JSON view
│   │   ├── endpoint.ex             # Modify: add socket
│   │   └── router.ex               # Modify: add API routes
│   └── collab_md_web.ex
├── test/
│   ├── collab_md/
│   │   ├── room_test.exs           # Create
│   │   └── room_supervisor_test.exs # Create
│   └── collab_md_web/
│       ├── channels/
│       │   └── room_channel_test.exs # Create
│       └── controllers/
│           └── room_controller_test.exs # Create
├── config/
├── Dockerfile                      # Create
├── fly.toml                        # Create
├── .dockerignore                   # Create
│
├── cli/                            # CLI escript (separate mix project)
│   ├── mix.exs                     # Create
│   ├── lib/
│   │   ├── collab_cli.ex           # Create: main/1 entry point
│   │   └── collab_cli/
│   │       ├── channel_client.ex   # Create: Phoenix Channel over WebSocket
│   │       └── sync.ex             # Create: file watch + bidirectional sync
│   └── config/
│       └── config.exs              # Create: default server URL
```

---

### Task 1: Scaffold Phoenix Project

**Files:**
- Create: `collab_md/` (via `mix phx.new`)
- Modify: `collab_md/config/dev.exs` (remove Ecto references)
- Modify: `collab_md/config/config.exs` (remove Ecto references)

- [ ] **Step 1: Generate Phoenix project**

```bash
cd /Users/justinwlin/Desktop
export PATH="/Users/justinwlin/.asdf/shims:$PATH"
mix phx.new collab_md --no-ecto --no-html --no-dashboard --no-mailer --no-live --no-assets
```

Accept defaults when prompted.

- [ ] **Step 2: Verify it compiles**

```bash
cd /Users/justinwlin/Desktop/collab_md
export PATH="/Users/justinwlin/.asdf/shims:$PATH"
mix deps.get && mix compile
```

Expected: compilation succeeds with 0 errors.

- [ ] **Step 3: Initialize git and commit**

```bash
cd /Users/justinwlin/Desktop/collab_md
git init
git add -A
git commit -m "chore: scaffold Phoenix project (API-only, no Ecto)"
```

---

### Task 2: Implement Room GenServer

**Files:**
- Create: `lib/collab_md/room.ex`
- Create: `test/collab_md/room_test.exs`

- [ ] **Step 1: Write Room tests**

```elixir
# test/collab_md/room_test.exs
defmodule CollabMd.RoomTest do
  use ExUnit.Case, async: true

  alias CollabMd.Room

  setup do
    code = "test-#{System.unique_integer([:positive])}"
    start_supervised!({Registry, keys: :unique, name: :"test_registry_#{code}"})
    # Start room directly for unit tests
    {:ok, pid} = Room.start_link(%{code: code, registry: CollabMd.RoomRegistry})
    %{code: code, pid: pid}
  end

  test "new room has empty document", %{code: code} do
    assert {:ok, ""} = Room.get_document(code)
  end

  test "update document creates a version", %{code: code} do
    assert {:ok, 1} = Room.update_document(code, "# Hello", "alice")
    assert {:ok, "# Hello"} = Room.get_document(code)
  end

  test "multiple updates create version history", %{code: code} do
    Room.update_document(code, "# v1", "alice")
    Room.update_document(code, "# v2", "bob")
    Room.update_document(code, "# v3", "alice")

    {:ok, versions} = Room.get_versions(code)
    assert length(versions) == 3
    assert hd(versions).number == 3
    assert hd(versions).author == "alice"
  end

  test "restore version rolls back document", %{code: code} do
    Room.update_document(code, "# Original", "alice")
    Room.update_document(code, "# Changed", "bob")

    {:ok, doc} = Room.restore_version(code, 1)
    assert doc == "# Original"
  end

  test "restore nonexistent version returns error", %{code: code} do
    assert {:error, :not_found} = Room.restore_version(code, 999)
  end

  test "join and leave track users", %{code: code} do
    Room.join(code, "alice")
    Room.join(code, "bob")
    {:ok, status} = Room.status(code)
    assert "alice" in status.users
    assert "bob" in status.users

    Room.leave(code, "alice")
    {:ok, status} = Room.status(code)
    refute "alice" in status.users
    assert "bob" in status.users
  end
end
```

- [ ] **Step 2: Run test to verify it fails**

```bash
mix test test/collab_md/room_test.exs
```

Expected: compilation error — `CollabMd.Room` not found.

- [ ] **Step 3: Implement Room GenServer**

```elixir
# lib/collab_md/room.ex
defmodule CollabMd.Room do
  use GenServer

  @idle_timeout :timer.hours(4)
  @empty_timeout :timer.minutes(30)

  defstruct [:code, :document, :versions, :users, :created_at, :idle_timer]

  # --- Client API ---

  def start_link(%{code: code} = opts) do
    registry = Map.get(opts, :registry, CollabMd.RoomRegistry)
    GenServer.start_link(__MODULE__, code, name: via(code, registry))
  end

  def via(code, registry \\ CollabMd.RoomRegistry) do
    {:via, Registry, {registry, code}}
  end

  def get_document(code), do: GenServer.call(via(code), :get_document)

  def update_document(code, content, author) do
    GenServer.call(via(code), {:update_document, content, author})
  end

  def get_versions(code), do: GenServer.call(via(code), :get_versions)

  def restore_version(code, number) do
    GenServer.call(via(code), {:restore_version, number})
  end

  def join(code, username), do: GenServer.call(via(code), {:join, username})
  def leave(code, username), do: GenServer.cast(via(code), {:leave, username})
  def status(code), do: GenServer.call(via(code), :status)

  # --- Server Callbacks ---

  @impl true
  def init(code) do
    timer = Process.send_after(self(), :idle_timeout, @idle_timeout)

    {:ok, %__MODULE__{
      code: code,
      document: "",
      versions: [],
      users: MapSet.new(),
      created_at: DateTime.utc_now(),
      idle_timer: timer
    }}
  end

  @impl true
  def handle_call(:get_document, _from, state) do
    {:reply, {:ok, state.document}, reset_timer(state)}
  end

  def handle_call({:update_document, content, author}, _from, state) do
    version_number = length(state.versions) + 1

    version = %{
      number: version_number,
      content: state.document,
      author: author,
      timestamp: DateTime.utc_now()
    }

    new_state = %{state | document: content, versions: [version | state.versions]}
    {:reply, {:ok, version_number}, reset_timer(new_state)}
  end

  def handle_call(:get_versions, _from, state) do
    versions =
      state.versions
      |> Enum.map(&Map.take(&1, [:number, :author, :timestamp]))
      |> Enum.sort_by(& &1.number, :desc)

    {:reply, {:ok, versions}, state}
  end

  def handle_call({:restore_version, number}, _from, state) do
    case Enum.find(state.versions, &(&1.number == number)) do
      nil ->
        {:reply, {:error, :not_found}, state}

      version ->
        restore = %{
          number: length(state.versions) + 1,
          content: state.document,
          author: "system:restore",
          timestamp: DateTime.utc_now()
        }

        new_state = %{state | document: version.content, versions: [restore | state.versions]}
        {:reply, {:ok, new_state.document}, reset_timer(new_state)}
    end
  end

  def handle_call({:join, username}, _from, state) do
    new_state = %{state | users: MapSet.put(state.users, username)}
    {:reply, :ok, reset_timer(new_state)}
  end

  def handle_call(:status, _from, state) do
    info = %{
      users: MapSet.to_list(state.users),
      version: length(state.versions),
      created_at: state.created_at
    }

    {:reply, {:ok, info}, state}
  end

  @impl true
  def handle_cast({:leave, username}, state) do
    new_users = MapSet.delete(state.users, username)

    timeout =
      if MapSet.size(new_users) == 0, do: @empty_timeout, else: @idle_timeout

    {:noreply, %{state | users: new_users} |> reset_timer(timeout)}
  end

  @impl true
  def handle_info(:idle_timeout, state) do
    {:stop, :normal, state}
  end

  defp reset_timer(state, timeout \\ @idle_timeout) do
    if state.idle_timer, do: Process.cancel_timer(state.idle_timer)
    timer = Process.send_after(self(), :idle_timeout, timeout)
    %{state | idle_timer: timer}
  end
end
```

- [ ] **Step 4: Run tests**

```bash
mix test test/collab_md/room_test.exs
```

Expected: all tests pass.

- [ ] **Step 5: Commit**

```bash
git add lib/collab_md/room.ex test/collab_md/room_test.exs
git commit -m "feat: implement Room GenServer with versioned snapshots"
```

---

### Task 3: Implement RoomSupervisor + Wire Application

**Files:**
- Create: `lib/collab_md/room_supervisor.ex`
- Modify: `lib/collab_md/application.ex`
- Create: `test/collab_md/room_supervisor_test.exs`

- [ ] **Step 1: Write RoomSupervisor tests**

```elixir
# test/collab_md/room_supervisor_test.exs
defmodule CollabMd.RoomSupervisorTest do
  use ExUnit.Case, async: false

  alias CollabMd.RoomSupervisor
  alias CollabMd.Room

  test "create_room returns a 6-char code" do
    {:ok, code} = RoomSupervisor.create_room()
    assert String.length(code) == 6
    assert Regex.match?(~r/^[a-f0-9]{6}$/, code)
  end

  test "created room is accessible" do
    {:ok, code} = RoomSupervisor.create_room()
    assert RoomSupervisor.room_exists?(code)
    assert {:ok, ""} = Room.get_document(code)
  end

  test "room_exists? returns false for nonexistent room" do
    refute RoomSupervisor.room_exists?("nope00")
  end
end
```

- [ ] **Step 2: Implement RoomSupervisor**

```elixir
# lib/collab_md/room_supervisor.ex
defmodule CollabMd.RoomSupervisor do
  use DynamicSupervisor

  def start_link(init_arg) do
    DynamicSupervisor.start_link(__MODULE__, init_arg, name: __MODULE__)
  end

  @impl true
  def init(_init_arg) do
    DynamicSupervisor.init(strategy: :one_for_one)
  end

  def create_room do
    code = generate_code()

    case DynamicSupervisor.start_child(__MODULE__, {CollabMd.Room, %{code: code}}) do
      {:ok, _pid} -> {:ok, code}
      {:error, reason} -> {:error, reason}
    end
  end

  def room_exists?(code) do
    case Registry.lookup(CollabMd.RoomRegistry, code) do
      [{_pid, _}] -> true
      [] -> false
    end
  end

  defp generate_code do
    :crypto.strong_rand_bytes(3) |> Base.encode16(case: :lower)
  end
end
```

- [ ] **Step 3: Update application.ex to start Registry + Supervisor**

Add to the children list in `lib/collab_md/application.ex`:

```elixir
children = [
  CollabMdWeb.Telemetry,
  {Registry, keys: :unique, name: CollabMd.RoomRegistry},
  {CollabMd.RoomSupervisor, []},
  {Phoenix.PubSub, name: CollabMd.PubSub},
  CollabMdWeb.Endpoint
]
```

- [ ] **Step 4: Run tests**

```bash
mix test test/collab_md/room_supervisor_test.exs
```

Expected: all pass.

- [ ] **Step 5: Commit**

```bash
git add lib/collab_md/room_supervisor.ex lib/collab_md/application.ex test/collab_md/room_supervisor_test.exs
git commit -m "feat: add RoomSupervisor + Registry to supervision tree"
```

---

### Task 4: Implement Phoenix Channel for Real-Time Sync

**Files:**
- Modify: `lib/collab_md_web/endpoint.ex` (add socket)
- Create: `lib/collab_md_web/channels/user_socket.ex`
- Create: `lib/collab_md_web/channels/room_channel.ex`
- Create: `test/collab_md_web/channels/room_channel_test.exs`

- [ ] **Step 1: Write channel tests**

```elixir
# test/collab_md_web/channels/room_channel_test.exs
defmodule CollabMdWeb.RoomChannelTest do
  use CollabMdWeb.ChannelCase

  alias CollabMd.RoomSupervisor

  setup do
    {:ok, code} = RoomSupervisor.create_room()
    {:ok, _, socket} =
      CollabMdWeb.UserSocket
      |> socket("user", %{username: "alice"})
      |> subscribe_and_join(CollabMdWeb.RoomChannel, "room:#{code}", %{"username" => "alice"})

    %{socket: socket, code: code}
  end

  test "joining sends current document state", %{socket: _socket, code: code} do
    # join reply contains current doc
    assert_push "doc:state", %{"document" => "", "version" => 0}
  end

  test "doc:update broadcasts to others", %{socket: socket, code: code} do
    push(socket, "doc:update", %{"document" => "# Hello", "author" => "alice"})
    assert_broadcast "doc:change", %{"document" => "# Hello", "author" => "alice", "version" => 1}
  end

  test "joining nonexistent room returns error" do
    assert {:error, %{reason: "room_not_found"}} =
      CollabMdWeb.UserSocket
      |> socket("user", %{})
      |> subscribe_and_join(CollabMdWeb.RoomChannel, "room:nonexistent", %{"username" => "bob"})
  end
end
```

- [ ] **Step 2: Create UserSocket**

```elixir
# lib/collab_md_web/channels/user_socket.ex
defmodule CollabMdWeb.UserSocket do
  use Phoenix.Socket

  channel "room:*", CollabMdWeb.RoomChannel

  @impl true
  def connect(params, socket, _connect_info) do
    {:ok, assign(socket, :username, Map.get(params, "username", "anonymous"))}
  end

  @impl true
  def id(socket), do: "user:#{socket.assigns.username}"
end
```

- [ ] **Step 3: Add socket to endpoint.ex**

Add this line inside `CollabMdWeb.Endpoint`:

```elixir
socket "/socket", CollabMdWeb.UserSocket,
  websocket: true,
  longpoll: false
```

- [ ] **Step 4: Implement RoomChannel**

```elixir
# lib/collab_md_web/channels/room_channel.ex
defmodule CollabMdWeb.RoomChannel do
  use CollabMdWeb, :channel

  alias CollabMd.{Room, RoomSupervisor}

  @impl true
  def join("room:" <> code, %{"username" => username}, socket) do
    if RoomSupervisor.room_exists?(code) do
      Room.join(code, username)
      socket = assign(socket, :code, code)
      socket = assign(socket, :username, username)
      send(self(), :after_join)
      {:ok, socket}
    else
      {:error, %{reason: "room_not_found"}}
    end
  end

  @impl true
  def handle_info(:after_join, socket) do
    code = socket.assigns.code
    {:ok, document} = Room.get_document(code)
    {:ok, status} = Room.status(code)

    push(socket, "doc:state", %{
      "document" => document,
      "version" => status.version
    })

    broadcast!(socket, "user:joined", %{
      "username" => socket.assigns.username,
      "users" => status.users
    })

    {:noreply, socket}
  end

  @impl true
  def handle_in("doc:update", %{"document" => content, "author" => author}, socket) do
    code = socket.assigns.code
    {:ok, version} = Room.update_document(code, content, author)

    broadcast_from!(socket, "doc:change", %{
      "document" => content,
      "author" => author,
      "version" => version
    })

    {:reply, {:ok, %{"version" => version}}, socket}
  end

  @impl true
  def terminate(_reason, socket) do
    if Map.has_key?(socket.assigns, :code) do
      Room.leave(socket.assigns.code, socket.assigns.username)

      case Room.status(socket.assigns.code) do
        {:ok, status} ->
          broadcast!(socket, "user:left", %{
            "username" => socket.assigns.username,
            "users" => status.users
          })
        _ -> :ok
      end
    end

    :ok
  end
end
```

- [ ] **Step 5: Run tests**

```bash
mix test test/collab_md_web/channels/room_channel_test.exs
```

Expected: all pass.

- [ ] **Step 6: Commit**

```bash
git add lib/collab_md_web/channels/ lib/collab_md_web/endpoint.ex test/collab_md_web/channels/
git commit -m "feat: add Phoenix Channel for real-time room sync"
```

---

### Task 5: Implement REST API

**Files:**
- Modify: `lib/collab_md_web/router.ex`
- Create: `lib/collab_md_web/controllers/room_controller.ex`
- Create: `lib/collab_md_web/controllers/room_json.ex`
- Create: `test/collab_md_web/controllers/room_controller_test.exs`

- [ ] **Step 1: Write controller tests**

```elixir
# test/collab_md_web/controllers/room_controller_test.exs
defmodule CollabMdWeb.RoomControllerTest do
  use CollabMdWeb.ConnCase

  alias CollabMd.RoomSupervisor

  describe "POST /api/rooms" do
    test "creates a room and returns code", %{conn: conn} do
      conn = post(conn, ~p"/api/rooms")
      assert %{"code" => code} = json_response(conn, 201)
      assert String.length(code) == 6
    end
  end

  describe "GET /api/rooms/:code/document" do
    test "returns current document", %{conn: conn} do
      {:ok, code} = RoomSupervisor.create_room()
      conn = get(conn, ~p"/api/rooms/#{code}/document")
      assert %{"document" => "", "version" => 0} = json_response(conn, 200)
    end

    test "returns 404 for nonexistent room", %{conn: conn} do
      conn = get(conn, ~p"/api/rooms/nope00/document")
      assert json_response(conn, 404)
    end
  end

  describe "PUT /api/rooms/:code/document" do
    test "updates document", %{conn: conn} do
      {:ok, code} = RoomSupervisor.create_room()

      conn = put(conn, ~p"/api/rooms/#{code}/document", %{
        "document" => "# Hello",
        "author" => "claude"
      })

      assert %{"version" => 1} = json_response(conn, 200)

      conn = get(build_conn(), ~p"/api/rooms/#{code}/document")
      assert %{"document" => "# Hello"} = json_response(conn, 200)
    end
  end

  describe "GET /api/rooms/:code/versions" do
    test "returns version history", %{conn: conn} do
      {:ok, code} = RoomSupervisor.create_room()
      CollabMd.Room.update_document(code, "v1", "alice")
      CollabMd.Room.update_document(code, "v2", "bob")

      conn = get(conn, ~p"/api/rooms/#{code}/versions")
      assert %{"versions" => versions} = json_response(conn, 200)
      assert length(versions) == 2
    end
  end

  describe "PUT /api/rooms/:code/restore/:version" do
    test "restores a previous version", %{conn: conn} do
      {:ok, code} = RoomSupervisor.create_room()
      CollabMd.Room.update_document(code, "original", "alice")
      CollabMd.Room.update_document(code, "changed", "bob")

      conn = put(conn, ~p"/api/rooms/#{code}/restore/1")
      assert %{"document" => "original"} = json_response(conn, 200)
    end
  end

  describe "GET /api/rooms/:code/status" do
    test "returns room status", %{conn: conn} do
      {:ok, code} = RoomSupervisor.create_room()
      conn = get(conn, ~p"/api/rooms/#{code}/status")
      assert %{"users" => [], "version" => 0} = json_response(conn, 200)
    end
  end
end
```

- [ ] **Step 2: Add routes to router.ex**

Add inside `lib/collab_md_web/router.ex`:

```elixir
scope "/api", CollabMdWeb do
  pipe_through :api

  post "/rooms", RoomController, :create
  get "/rooms/:code/document", RoomController, :get_document
  put "/rooms/:code/document", RoomController, :update_document
  get "/rooms/:code/versions", RoomController, :versions
  put "/rooms/:code/restore/:version", RoomController, :restore
  get "/rooms/:code/status", RoomController, :status
end
```

- [ ] **Step 3: Implement RoomController**

```elixir
# lib/collab_md_web/controllers/room_controller.ex
defmodule CollabMdWeb.RoomController do
  use CollabMdWeb, :controller

  alias CollabMd.{Room, RoomSupervisor}

  def create(conn, _params) do
    {:ok, code} = RoomSupervisor.create_room()
    conn |> put_status(:created) |> json(%{code: code})
  end

  def get_document(conn, %{"code" => code}) do
    with true <- RoomSupervisor.room_exists?(code),
         {:ok, document} <- Room.get_document(code),
         {:ok, status} <- Room.status(code) do
      json(conn, %{document: document, version: status.version})
    else
      false -> conn |> put_status(:not_found) |> json(%{error: "room_not_found"})
    end
  end

  def update_document(conn, %{"code" => code, "document" => content} = params) do
    author = Map.get(params, "author", "anonymous")

    with true <- RoomSupervisor.room_exists?(code),
         {:ok, version} <- Room.update_document(code, content, author) do
      # Broadcast to channel subscribers
      CollabMdWeb.Endpoint.broadcast!("room:#{code}", "doc:change", %{
        "document" => content,
        "author" => author,
        "version" => version
      })

      json(conn, %{version: version})
    else
      false -> conn |> put_status(:not_found) |> json(%{error: "room_not_found"})
    end
  end

  def versions(conn, %{"code" => code}) do
    with true <- RoomSupervisor.room_exists?(code),
         {:ok, versions} <- Room.get_versions(code) do
      json(conn, %{versions: versions})
    else
      false -> conn |> put_status(:not_found) |> json(%{error: "room_not_found"})
    end
  end

  def restore(conn, %{"code" => code, "version" => version_str}) do
    version = String.to_integer(version_str)

    with true <- RoomSupervisor.room_exists?(code),
         {:ok, document} <- Room.restore_version(code, version) do
      CollabMdWeb.Endpoint.broadcast!("room:#{code}", "doc:change", %{
        "document" => document,
        "author" => "system:restore",
        "version" => version
      })

      json(conn, %{document: document})
    else
      false -> conn |> put_status(:not_found) |> json(%{error: "room_not_found"})
      {:error, :not_found} -> conn |> put_status(:not_found) |> json(%{error: "version_not_found"})
    end
  end

  def status(conn, %{"code" => code}) do
    with true <- RoomSupervisor.room_exists?(code),
         {:ok, status} <- Room.status(code) do
      json(conn, status)
    else
      false -> conn |> put_status(:not_found) |> json(%{error: "room_not_found"})
    end
  end
end
```

- [ ] **Step 4: Run tests**

```bash
mix test test/collab_md_web/controllers/room_controller_test.exs
```

Expected: all pass.

- [ ] **Step 5: Commit**

```bash
git add lib/collab_md_web/router.ex lib/collab_md_web/controllers/ test/collab_md_web/controllers/
git commit -m "feat: add REST API for room CRUD and document operations"
```

---

### Task 6: Implement CLI Client

**Files:**
- Create: `cli/mix.exs`
- Create: `cli/config/config.exs`
- Create: `cli/lib/collab_cli.ex`
- Create: `cli/lib/collab_cli/channel_client.ex`
- Create: `cli/lib/collab_cli/sync.ex`

- [ ] **Step 1: Create CLI mix project**

```bash
cd /Users/justinwlin/Desktop/collab_md
mkdir -p cli
cd cli
mix new collab_cli
```

- [ ] **Step 2: Update cli/mix.exs with deps and escript config**

```elixir
# cli/mix.exs
defmodule CollabCli.MixProject do
  use Mix.Project

  def project do
    [
      app: :collab_cli,
      version: "0.1.0",
      elixir: "~> 1.17",
      start_permanent: Mix.env() == :prod,
      deps: deps(),
      escript: [main_module: CollabCli]
    ]
  end

  def application do
    [
      extra_applications: [:logger]
    ]
  end

  defp deps do
    [
      {:websockex, "~> 0.4.3"},
      {:jason, "~> 1.4"},
      {:file_system, "~> 1.0"},
      {:req, "~> 0.5"}
    ]
  end
end
```

- [ ] **Step 3: Create config**

```elixir
# cli/config/config.exs
import Config

config :collab_cli,
  server_url: System.get_env("COLLAB_SERVER") || "http://localhost:4000"
```

- [ ] **Step 4: Implement ChannelClient (Phoenix Channel over WebSocket)**

```elixir
# cli/lib/collab_cli/channel_client.ex
defmodule CollabCli.ChannelClient do
  use WebSockex

  @heartbeat_interval 30_000

  defstruct [:url, :topic, :username, :ref, :join_ref, :callback_pid]

  def start_link(opts) do
    url = opts[:url]
    state = %__MODULE__{
      url: url,
      topic: opts[:topic],
      username: opts[:username],
      ref: 0,
      join_ref: nil,
      callback_pid: opts[:callback_pid]
    }

    ws_url = url
      |> String.replace("http://", "ws://")
      |> String.replace("https://", "wss://")
      |> Kernel.<>("/socket/websocket?username=#{opts[:username]}")

    WebSockex.start_link(ws_url, __MODULE__, state)
  end

  def send_update(pid, document, author) do
    WebSockex.cast(pid, {:send_update, document, author})
  end

  # --- Callbacks ---

  @impl true
  def handle_connect(_conn, state) do
    # Join the channel
    ref = state.ref + 1
    join_msg = Jason.encode!(%{
      topic: state.topic,
      event: "phx_join",
      payload: %{username: state.username},
      ref: to_string(ref)
    })

    # Schedule heartbeat
    Process.send_after(self(), :heartbeat, @heartbeat_interval)

    {:reply, {:text, join_msg}, %{state | ref: ref, join_ref: to_string(ref)}}
  end

  @impl true
  def handle_frame({:text, msg}, state) do
    case Jason.decode!(msg) do
      %{"event" => "doc:state", "payload" => payload} ->
        send(state.callback_pid, {:doc_state, payload})
        {:ok, state}

      %{"event" => "doc:change", "payload" => payload} ->
        send(state.callback_pid, {:doc_change, payload})
        {:ok, state}

      %{"event" => "user:joined", "payload" => payload} ->
        send(state.callback_pid, {:user_joined, payload})
        {:ok, state}

      %{"event" => "user:left", "payload" => payload} ->
        send(state.callback_pid, {:user_left, payload})
        {:ok, state}

      %{"event" => "phx_reply", "payload" => %{"status" => "ok"}} ->
        {:ok, state}

      %{"event" => "phx_reply", "payload" => %{"status" => "error", "response" => resp}} ->
        send(state.callback_pid, {:error, resp})
        {:ok, state}

      _ ->
        {:ok, state}
    end
  end

  @impl true
  def handle_cast({:send_update, document, author}, state) do
    ref = state.ref + 1
    msg = Jason.encode!(%{
      topic: state.topic,
      event: "doc:update",
      payload: %{document: document, author: author},
      ref: to_string(ref)
    })

    {:reply, {:text, msg}, %{state | ref: ref}}
  end

  @impl true
  def handle_info(:heartbeat, state) do
    ref = state.ref + 1
    msg = Jason.encode!(%{
      topic: "phoenix",
      event: "heartbeat",
      payload: %{},
      ref: to_string(ref)
    })

    Process.send_after(self(), :heartbeat, @heartbeat_interval)
    {:reply, {:text, msg}, %{state | ref: ref}}
  end

  def handle_info(_msg, state), do: {:ok, state}

  @impl true
  def handle_disconnect(_reason, state) do
    IO.puts("[collab] Disconnected. Reconnecting in 2s...")
    Process.sleep(2000)
    {:reconnect, state}
  end
end
```

- [ ] **Step 5: Implement Sync (file watcher + bidirectional sync)**

```elixir
# cli/lib/collab_cli/sync.ex
defmodule CollabCli.Sync do
  use GenServer

  defstruct [:file_path, :code, :username, :channel_pid, :watcher_pid, :last_content, :skip_next_write]

  def start_link(opts) do
    GenServer.start_link(__MODULE__, opts, name: __MODULE__)
  end

  @impl true
  def init(opts) do
    file_path = opts[:file_path]
    File.write!(file_path, opts[:initial_content] || "")

    {:ok, watcher_pid} = FileSystem.start_link(dirs: [Path.dirname(file_path)])
    FileSystem.subscribe(watcher_pid)

    state = %__MODULE__{
      file_path: file_path,
      code: opts[:code],
      username: opts[:username],
      channel_pid: opts[:channel_pid],
      watcher_pid: watcher_pid,
      last_content: opts[:initial_content] || "",
      skip_next_write: false
    }

    {:ok, state}
  end

  @impl true
  def handle_info({:file_event, _pid, {path, events}}, state) do
    if Path.basename(path) == Path.basename(state.file_path) and :modified in events do
      if state.skip_next_write do
        {:noreply, %{state | skip_next_write: false}}
      else
        case File.read(state.file_path) do
          {:ok, content} when content != state.last_content ->
            CollabCli.ChannelClient.send_update(state.channel_pid, content, state.username)
            {:noreply, %{state | last_content: content}}

          _ ->
            {:noreply, state}
        end
      end
    else
      {:noreply, state}
    end
  end

  def handle_info({:file_event, _pid, :stop}, state) do
    {:noreply, state}
  end

  # Called by main process when receiving channel updates
  def handle_info({:remote_update, content}, state) do
    if content != state.last_content do
      File.write!(state.file_path, content)
      {:noreply, %{state | last_content: content, skip_next_write: true}}
    else
      {:noreply, state}
    end
  end

  def remote_update(content) do
    send(__MODULE__, {:remote_update, content})
  end
end
```

- [ ] **Step 6: Implement main CLI entry point**

```elixir
# cli/lib/collab_cli.ex
defmodule CollabCli do
  @moduledoc "CLI for CollabMd — live collaborative markdown editing."

  def main(args) do
    Application.ensure_all_started(:req)

    case args do
      ["create" | rest] -> cmd_create(rest)
      ["join", code | rest] -> cmd_join(code, rest)
      ["history", code] -> cmd_history(code)
      ["restore", code, version] -> cmd_restore(code, version)
      ["status", code] -> cmd_status(code)
      _ -> usage()
    end
  end

  defp server_url do
    Application.get_env(:collab_cli, :server_url, "http://localhost:4000")
  end

  defp parse_opts(args, defaults \\ %{}) do
    args
    |> Enum.chunk_every(2)
    |> Enum.reduce(defaults, fn
      ["--name", name], acc -> Map.put(acc, :name, name)
      ["--file", file], acc -> Map.put(acc, :file, file)
      ["--server", url], acc -> Map.put(acc, :server, url)
      _, acc -> acc
    end)
  end

  defp cmd_create(rest) do
    opts = parse_opts(rest, %{name: System.get_env("USER", "anonymous")})

    case Req.post("#{server_url()}/api/rooms") do
      {:ok, %{status: 201, body: %{"code" => code}}} ->
        IO.puts("Room created: #{code}")
        IO.puts("Share this code: collab join #{code} --name <name>")
        IO.puts("")
        cmd_join(code, ["--name", opts.name])

      {:error, reason} ->
        IO.puts("Error creating room: #{inspect(reason)}")
    end
  end

  defp cmd_join(code, rest) do
    opts = parse_opts(rest, %{name: System.get_env("USER", "anonymous")})
    file_path = Path.absname(Map.get(opts, :file, "collab-#{code}.md"))

    IO.puts("Joining room #{code} as #{opts.name}...")
    IO.puts("Syncing to: #{file_path}")
    IO.puts("Edit the file with any editor. Changes sync automatically.")
    IO.puts("Press Ctrl+C to leave.\n")

    # Start channel client
    {:ok, channel_pid} = CollabCli.ChannelClient.start_link(%{
      url: server_url(),
      topic: "room:#{code}",
      username: opts.name,
      callback_pid: self()
    })

    # Wait for initial state
    initial_content = receive do
      {:doc_state, %{"document" => doc}} ->
        IO.puts("[collab] Connected! Document version: #{Map.get(%{}, "version", 0)}")
        doc
      {:error, reason} ->
        IO.puts("[collab] Error: #{inspect(reason)}")
        System.halt(1)
    after
      10_000 ->
        IO.puts("[collab] Timeout connecting to room.")
        System.halt(1)
    end

    # Start file sync
    {:ok, _sync_pid} = CollabCli.Sync.start_link(%{
      file_path: file_path,
      code: code,
      username: opts.name,
      channel_pid: channel_pid,
      initial_content: initial_content
    })

    # Event loop
    listen_loop()
  end

  defp listen_loop do
    receive do
      {:doc_change, %{"document" => content, "author" => author, "version" => version}} ->
        IO.puts("[collab] Update from #{author} (v#{version})")
        CollabCli.Sync.remote_update(content)
        listen_loop()

      {:user_joined, %{"username" => name, "users" => users}} ->
        IO.puts("[collab] #{name} joined (online: #{Enum.join(users, ", ")})")
        listen_loop()

      {:user_left, %{"username" => name, "users" => users}} ->
        IO.puts("[collab] #{name} left (online: #{Enum.join(users, ", ")})")
        listen_loop()

      {:doc_state, %{"document" => content}} ->
        CollabCli.Sync.remote_update(content)
        listen_loop()

      {:error, reason} ->
        IO.puts("[collab] Error: #{inspect(reason)}")
        listen_loop()
    end
  end

  defp cmd_history(code) do
    case Req.get("#{server_url()}/api/rooms/#{code}/versions") do
      {:ok, %{status: 200, body: %{"versions" => versions}}} ->
        if versions == [] do
          IO.puts("No versions yet.")
        else
          IO.puts("Version history for room #{code}:\n")
          Enum.each(versions, fn v ->
            IO.puts("  v#{v["number"]}  by #{v["author"]}  at #{v["timestamp"]}")
          end)
        end

      {:ok, %{status: 404}} ->
        IO.puts("Room #{code} not found.")

      {:error, reason} ->
        IO.puts("Error: #{inspect(reason)}")
    end
  end

  defp cmd_restore(code, version) do
    case Req.put("#{server_url()}/api/rooms/#{code}/restore/#{version}") do
      {:ok, %{status: 200, body: %{"document" => doc}}} ->
        IO.puts("Restored to version #{version}. Document:\n")
        IO.puts(doc)

      {:ok, %{status: 404}} ->
        IO.puts("Room or version not found.")

      {:error, reason} ->
        IO.puts("Error: #{inspect(reason)}")
    end
  end

  defp cmd_status(code) do
    case Req.get("#{server_url()}/api/rooms/#{code}/status") do
      {:ok, %{status: 200, body: status}} ->
        IO.puts("Room: #{code}")
        IO.puts("Version: #{status["version"]}")
        IO.puts("Online: #{Enum.join(status["users"], ", ")}")

      {:ok, %{status: 404}} ->
        IO.puts("Room #{code} not found.")

      {:error, reason} ->
        IO.puts("Error: #{inspect(reason)}")
    end
  end

  defp usage do
    IO.puts("""
    CollabMd CLI — Live collaborative markdown editing

    Usage:
      collab create [--name NAME]              Create a room and start syncing
      collab join CODE [--name NAME]           Join an existing room
      collab history CODE                      Show version history
      collab restore CODE VERSION              Restore a previous version
      collab status CODE                       Show room status

    Options:
      --name NAME      Your display name (default: $USER)
      --file PATH      Local file path (default: collab-CODE.md)
      --server URL     Server URL (default: $COLLAB_SERVER or http://localhost:4000)

    Environment:
      COLLAB_SERVER    Server URL override
    """)
  end
end
```

- [ ] **Step 7: Fetch deps and build escript**

```bash
cd /Users/justinwlin/Desktop/collab_md/cli
mix deps.get && mix compile && mix escript.build
```

Expected: produces `collab_cli` executable.

- [ ] **Step 8: Commit**

```bash
cd /Users/justinwlin/Desktop/collab_md
git add cli/
git commit -m "feat: add CLI client with file sync and WebSocket channel"
```

---

### Task 7: Fly.io Deployment Config

**Files:**
- Create: `Dockerfile`
- Create: `.dockerignore`
- Create: `fly.toml`
- Modify: `config/runtime.exs`

- [ ] **Step 1: Create Dockerfile**

```dockerfile
# Dockerfile
ARG ELIXIR_VERSION=1.17.3
ARG OTP_VERSION=27.1
ARG DEBIAN_VERSION=bookworm-20240904-slim

ARG BUILDER_IMAGE="hexpm/elixir:${ELIXIR_VERSION}-erlang-${OTP_VERSION}-debian-${DEBIAN_VERSION}"
ARG RUNNER_IMAGE="debian:${DEBIAN_VERSION}"

FROM ${BUILDER_IMAGE} AS builder

ENV MIX_ENV="prod"
WORKDIR /app

RUN mix local.hex --force && mix local.rebar --force

COPY mix.exs mix.lock ./
RUN mix deps.get --only $MIX_ENV && mix deps.compile

COPY config/config.exs config/${MIX_ENV}.exs config/runtime.exs config/
COPY lib lib

RUN mix compile && mix release

# --- Runner ---
FROM ${RUNNER_IMAGE}

RUN apt-get update -y && apt-get install -y libstdc++6 openssl libncurses5 locales \
  && apt-get clean && rm -f /var/lib/apt/lists/*_*

ENV LANG=en_US.UTF-8
ENV LANGUAGE=en_US:en
ENV LC_ALL=en_US.UTF-8
RUN sed -i '/en_US.UTF-8/s/^# //g' /etc/locale.gen && locale-gen

WORKDIR "/app"
RUN chown nobody /app
ENV MIX_ENV="prod"

COPY --from=builder --chown=nobody:root /app/_build/${MIX_ENV}/rel/collab_md ./

USER nobody

CMD ["/app/bin/server"]
```

- [ ] **Step 2: Create .dockerignore**

```
cli/
.git/
_build/
deps/
doc/
test/
.env
*.md
```

- [ ] **Step 3: Create fly.toml**

```toml
# fly.toml
app = "collab-md"
primary_region = "iad"

[build]

[env]
  PHX_HOST = "collab-md.fly.dev"
  PORT = "8080"

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = "stop"
  auto_start_machines = true
  min_machines_running = 0

  [http_service.concurrency]
    type = "connections"
    hard_limit = 1000
    soft_limit = 500

[[vm]]
  memory = "256mb"
  cpu_kind = "shared"
  cpus = 1
```

- [ ] **Step 4: Update config/runtime.exs for production**

Ensure `config/runtime.exs` has:

```elixir
if config_env() == :prod do
  secret_key_base =
    System.get_env("SECRET_KEY_BASE") ||
      raise "SECRET_KEY_BASE not set"

  host = System.get_env("PHX_HOST") || "localhost"
  port = String.to_integer(System.get_env("PORT") || "4000")

  config :collab_md, CollabMdWeb.Endpoint,
    url: [host: host, port: 443, scheme: "https"],
    http: [ip: {0, 0, 0, 0}, port: port],
    server: true,
    secret_key_base: secret_key_base,
    check_origin: false
end
```

- [ ] **Step 5: Commit**

```bash
git add Dockerfile .dockerignore fly.toml config/runtime.exs
git commit -m "feat: add Fly.io deployment config"
```

---

### Task 8: Full Integration Smoke Test

- [ ] **Step 1: Run all server tests**

```bash
cd /Users/justinwlin/Desktop/collab_md
mix test
```

Expected: all tests pass.

- [ ] **Step 2: Start server and test manually**

```bash
mix phx.server &
# In another terminal:
# Create room
curl -X POST http://localhost:4000/api/rooms
# Update document
curl -X PUT http://localhost:4000/api/rooms/CODE/document \
  -H 'Content-Type: application/json' \
  -d '{"document": "# Hello World", "author": "test"}'
# Read it back
curl http://localhost:4000/api/rooms/CODE/document
# Check versions
curl http://localhost:4000/api/rooms/CODE/versions
```

- [ ] **Step 3: Test CLI**

```bash
cd /Users/justinwlin/Desktop/collab_md/cli
./collab_cli create --name justin
# In another terminal:
./collab_cli join CODE --name gf
```

- [ ] **Step 4: Final commit**

```bash
git add -A
git commit -m "chore: integration smoke test passed"
```
